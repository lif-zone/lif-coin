Session log
===========

USER: please do task in browser/tasks.txt of "multi-wallet"

[tasks.txt content:]
make wallet.tsx support multiple wallets
- today it is just a single wallet. It should be "boxes" of wallets,
  and a box for "New wallet". So when starting it first time it only has
  "New wallet" box. clicking on it - adds a new wallet, where you select
  all the things you sellect in today's wallet.tsx implementation:
  Network (Bitcoin, Bitcoin testnet, Lif), the wallet key etc...
- Once a wallet is added, in its box is the amount of unspent coins it has,
  and amount of BTC/LIF it has.
- clicking on it - opens up the wallet's transactions, and allows you to
  delete the wallet, and to send and receive from this wallet.
- in the global settings, you can set the electrumx server for the different
  networks (Bitcoin, Bitcoin testnet, Lif...)

CLAUDE: [Read wallet.tsx, app.tsx, main.tsx, style.css, then wrote new wallet.tsx with multi-wallet support]

Done. The new wallet.tsx supports:
- Home screen with wallet boxes + "New Wallet" box
- Each wallet card shows balance and UTXO count, fetched on mount
- Clicking a wallet opens WalletDetailScreen (transactions, send, backup, delete)
- AddWalletScreen: choose network, generate new or restore from mnemonic
- SettingsScreen: configure ElectrumX server URLs per network
- localStorage migration from old single-wallet format
- Wallets stored as array under 'wallets' key

---

USER: but keep coding conventions the same - no spaces before/after arrow functions (key, conf)=>( ... )

CLAUDE: [Rewrote wallet.tsx with no spaces around => throughout]

---

